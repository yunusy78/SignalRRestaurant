@model List<DtoLayer.ContactDtos.ResultContactDto>

@{
    ViewBag.Title = "title";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";
}

<div class="main-panel">
<div class="content">
    <div class="container-fluid">
        <h4 class="page-title">Kontakt</h4>
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">Kontakt Liste</div>
                    </div>
                    <div class="card-body">
                        <table id="datatable-buttons" class="table table-bordered table-bordered-bd-info mt-4">
                            <thead>
                            <tr>
                                <th>Id</th>
                                <th>Lokasjon</th>
                                <th>E-Post</th>
                                <th>Telefon</th>
                                <th>Beskrivelse</th>
                                <th>Rediger</th>
                                <th>Slett</th>
                                <th>Detaljer</th>
                            </tr>
                            </thead>

                            <tbody>
                            @foreach(var item in Model)
                            {
                                <tr>
                                    <td>@item.ContactId</td>
                                    <td>@item.Location</td>
                                    <td>@item.Email</td>
                                    <td>@item.Phone</td>
                                    <td>@item.FooterDescription</td>
                                    <td><a href="/Admin/AdminContact/Update/@item.ContactId" class="btn btn-outline-warning"> <i class="mdi mdi-account-edit"></i> Rediger</a></td>
                                    <td><a href="/Admin/AdminContact/Delete/@item.ContactId" class="btn btn-outline-danger" onclick="deleteItemWarning()"><i class="mdi mdi-delete"></i> Slett</a> </td>
                                    <td><a href="#" class="btn btn-outline-primary"><i class="mdi mdi-eye"></i> Detaljer</a></td>
                                </tr>
                        
                                <script>

        function deleteItemWarning() {
        var confirmDelete = confirm("Advarsel! Denne handlingen vil slette elementet permanent. Er du sikker på at du vil fortsette?");
        if (confirmDelete) {
            // Brukeren bekreftet sletting, slettingsprosessen kan fortsette.
            window.location.href = "/Admin/AdminContact/Delete/@item.ContactId";
        }
    }
            </script>
                            }
                            </tbody>
                        </table>
                        <a href="/Admin/AdminContact/Create" class="btn btn-primary"> <i class="mdi mdi-plus"></i> Legg en ny Kontakt</a>
                        <button class="btn btn-primary" onclick="goBack()"><i class="bx bx-arrow-back"></i> Tilbake</button>
                    </div> <!-- end card body-->
                </div> <!-- end card -->
            </div><!-- end col-->
        </div>
    </div>
    </div>
</div>